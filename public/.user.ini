; One featured image plus a gallery of up to 60 images.
max_file_uploads = 100

; Gallery images are limited by Laravel to 2 MB each. This includes request overhead.
upload_max_filesize = 3M
post_max_size = 160M
